# empusa/templates/

This directory is reserved for future external template files used by the
module scaffolding system (`empusa.cli_modules.create_module_template`).

Currently all boilerplate templates (C, C++, C#, Rust, Go, Perl) are
defined inline within `cli_modules.py`.  As the template library grows,
individual files can be placed here and loaded at runtime.
